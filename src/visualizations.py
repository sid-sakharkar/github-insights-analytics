import matplotlib.pyplot as plt\nimport seaborn as sns\nimport plotly.express as px\n\n\ndef plot_bar_chart(data, x, y, title='Bar Chart', xlabel='X-axis', ylabel='Y-axis'):\n    plt.figure(figsize=(10, 6))\n    plt.bar(data[x], data[y], color='blue')\n    plt.title(title)\n    plt.xlabel(xlabel)\n    plt.ylabel(ylabel)\n    plt.xticks(rotation=45)\n    plt.tight_layout()\n    plt.show()\n\ndef plot_line_chart(data, x, y, title='Line Chart', xlabel='X-axis', ylabel='Y-axis'):\n    plt.figure(figsize=(10, 6))\n    plt.plot(data[x], data[y], marker='o', color='orange')\n    plt.title(title)\n    plt.xlabel(xlabel)\n    plt.ylabel(ylabel)\n    plt.xticks(rotation=45)\n    plt.grid(True)\n    plt.tight_layout()\n    plt.show()\n\ndef plot_heatmap(data, title='Heatmap'):\n    plt.figure(figsize=(12, 8))\n    sns.heatmap(data.corr(), annot=True, fmt='.2f', cmap='coolwarm')\n    plt.title(title)\n    plt.tight_layout()\n    plt.show()\n\ndef plot_interactive_scatter(data, x, y, title='Interactive Scatter Plot'):\n    fig = px.scatter(data, x=x, y=y, title=title)\n    fig.show()\n